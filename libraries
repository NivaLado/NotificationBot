pip install aiogram
pip install timezonefinder
pip install geopy
pip install geocoder


1) Отображение всех напоминаний
2) Удаление напоминания
3) Установка часового пояся и напоминаия о нём если он не установленн
    При выборе часового пояса есть вариант выбрать с помощью геолокации либо ручной ввод
4) Добавить интерактивные кнопки
5) Написать парсер текста

!) Проверить что бы нельзя было поставить на одно и то же время

